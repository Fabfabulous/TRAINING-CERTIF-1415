<%= simple_form_for [@vehicle, @booking] do |f| %>
  <%= f.input :start_date, as: :datetime %>
  <%= f.input :end_date, as: :datetime%>
  <%= f.button :submit, class: "btn btn-gradient btn-sm pt-1" %>
<% end %>
